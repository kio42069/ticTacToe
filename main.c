#include <stdio.h>
#include <stdlib.h>

//checks to see if a player has won or if game is draw
int checkForWin();

//redraws the board for each turn
void drawBoard();

//sets the char array with a selection and check for an invalid selection
void markBoard();

int main()
{
    return 0;
}

int checkForWin(char arr[9])
{
    if(arr)
}
